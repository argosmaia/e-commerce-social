spring:
  application:
    name: E-Commerce-mvp
    
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommercesocial
    username: argosdgti
    password: "12345678" 
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

security:
  jwt:
    # Defini valores padrão para dev. Se tiver outros, pode trocar.
    secret: minha_chave_secreta_super_segura_para_desenvolvimento_local_123
    issuer: e-commerce-api
    expiration: 86400000 # 24 horas em milissegundos